<section class="hero">
  <div class="hero-text">
    <div class="reveal"><span class="section-label">Développeur Web</span></div>
    <h1 class="reveal" style="transition-delay: 0.1s">
      Je crée des sites web qui <br />
      <span class="text-gradient">boostent</span> votre commerce local
    </h1>
    <p class="reveal" style="transition-delay: 0.2s; color: var(--muted); font-size: 1.05rem; line-height: 1.8; max-width: 480px; margin-bottom: 36px;">
      Sites vitrines, e-commerce et applications sur mesure.
      Design impactant, performance optimale, résultat tangible.
    </p>
    <div class="reveal hero-btns" style="transition-delay: 0.3s">
      <a href="/projets" class="btn-primary">
        <i class="fa-solid fa-arrow-right"></i>
        Voir mes projets
      </a>
      <a href="https://wa.me/22966153442" class="btn-outline" target="_blank">
        <i class="fa-brands fa-whatsapp"></i>
        Discutons
      </a>
    </div>
    <div class="reveal hero-stats" style="transition-delay: 0.4s">
      <div><strong data-count="20">0</strong><span>projets</span></div>
      <div><strong data-count="15">0</strong><span>clients</span></div>
      <div><strong data-count="5">0</strong><span>ans d'expertise</span></div>
    </div>
  </div>
  <div class="hero-visual">
    <div class="geo-pattern">
      <div class="geo-circle geo-1"></div>
      <div class="geo-circle geo-2"></div>
      <div class="geo-circle geo-3"></div>
      <div class="geo-dots"></div>
    </div>
    <div class="hero-code">
      <span class="code-comment">// Projet récent</span>
      <span class="code-line">&lt;<span style="color: #bd3808;">section</span> <span style="color: #e04e1a;">class</span>=<span style="color: #10b981;">"hero"</span>&gt;</span>
      <span class="code-line">  &lt;<span style="color: #bd3808;">h1</span>&gt;Site vitrine&lt;/<span style="color: #bd3808;">h1</span>&gt;</span>
      <span class="code-line">  &lt;<span style="color: #bd3808;">p</span>&gt;Design moderne, SEO friendly&lt;/<span style="color: #bd3808;">p</span>&gt;</span>
      <span class="code-line">&lt;/<span style="color: #bd3808;">section</span>&gt;</span>
    </div>
  </div>
</section>

<style>
  .hero {
    min-height: 100vh;
    display: grid;
    grid-template-columns: 1fr 1fr;
    align-items: center;
    padding: 0 80px;
    max-width: 1400px;
    margin: 0 auto;
    gap: 60px;
    position: relative;
  }
  .hero-text { padding-top: 80px; }
  .hero h1 {
    font-family: 'Playfair Display', serif;
    font-size: clamp(2.4rem, 5vw, 4rem);
    font-weight: 700;
    line-height: 1.1;
    margin: 20px 0;
  }
  .hero-btns { display: flex; gap: 16px; flex-wrap: wrap; }
  .hero-stats {
    display: flex;
    gap: 48px;
    margin-top: 50px;
    padding-top: 30px;
    border-top: 1px solid rgba(255,255,255,0.08);
  }
  .hero-stats div { display: flex; flex-direction: column; }
  .hero-stats strong {
    font-family: 'Playfair Display', serif;
    font-size: 2rem;
    color: var(--brand);
  }
  .hero-stats span {
    font-size: 0.8rem;
    color: var(--muted);
    text-transform: uppercase;
    letter-spacing: 0.08em;
  }

  .hero-visual {
    position: relative;
    display: flex;
    align-items: center;
    justify-content: center;
    min-height: 500px;
  }
  .geo-pattern {
    position: absolute;
    inset: 0;
    display: flex;
    align-items: center;
    justify-content: center;
  }
  .geo-circle {
    position: absolute;
    border-radius: 50%;
    border: 1px solid rgba(189, 56, 8, 0.15);
  }
  .geo-1 { width: 400px; height: 400px; animation: spinSlow 25s linear infinite; }
  .geo-2 { width: 300px; height: 300px; animation: spinSlow 20s linear infinite reverse; }
  .geo-3 { width: 200px; height: 200px; animation: spinSlow 15s linear infinite; }
  .geo-dots {
    position: absolute;
    width: 100%;
    height: 100%;
    background-image: radial-gradient(circle, rgba(189, 56, 8, 0.08) 1px, transparent 1px);
    background-size: 24px 24px;
  }

  .hero-code {
    position: relative;
    z-index: 2;
    background: rgba(26, 26, 26, 0.8);
    border: 1px solid rgba(255,255,255,0.08);
    border-radius: 12px;
    padding: 28px 32px;
    display: flex;
    flex-direction: column;
    gap: 8px;
    font-family: 'JetBrains Mono', monospace;
    font-size: 0.85rem;
    backdrop-filter: blur(12px);
    max-width: 380px;
    width: 100%;
  }
  .code-comment { color: #6b7280; }
  .code-line { color: rgba(255,255,255,0.7); }

  @media (max-width: 992px) {
    .hero {
      grid-template-columns: 1fr;
      padding: 100px 24px 40px;
      gap: 40px;
    }
    .hero-visual { min-height: 350px; }
    .hero-code { max-width: 100%; }
    .geo-1 { width: 300px; height: 300px; }
    .geo-2 { width: 220px; height: 220px; }
    .geo-3 { width: 150px; height: 150px; }
  }
</style>
