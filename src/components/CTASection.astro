<section class="cta">
  <div class="cta-inner">
    <div class="cta-glow"></div>
    <span class="section-label" style="justify-content: center;">Prêt à démarrer ?</span>
    <h2 class="cta-title">
      Un projet en tête ? <span class="text-gradient">Discutons-en</span>
    </h2>
    <p class="cta-sub">
      Que vous ayez une idée précise ou besoin de conseils, je suis là
      pour vous accompagner. Premier échange sans engagement.
    </p>
    <div class="cta-btns">
      <a href="https://wa.me/22966153442" class="cta-btn" target="_blank">
        <i class="fa-brands fa-whatsapp"></i>
        WhatsApp
      </a>
      <a href="/contact" class="cta-btn-outline">
        <i class="fa-regular fa-envelope"></i>
        Formulaire
      </a>
    </div>
  </div>
</section>

<style>
  .cta {
    padding: 0 80px 100px;
    max-width: 1400px;
    margin: 0 auto;
  }
  .cta-inner {
    position: relative;
    background: linear-gradient(135deg, #1a1a1a, #0a0a0a);
    border: 1px solid rgba(189, 56, 8, 0.15);
    border-radius: 24px;
    padding: 80px 60px;
    text-align: center;
    overflow: hidden;
  }
  .cta-glow {
    position: absolute;
    width: 400px;
    height: 400px;
    background: radial-gradient(circle, rgba(189, 56, 8, 0.08) 0%, transparent 70%);
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    pointer-events: none;
  }
  .cta-title {
    font-family: 'Playfair Display', serif;
    font-size: clamp(1.8rem, 3.5vw, 2.8rem);
    font-weight: 700;
    margin: 12px 0 16px;
  }
  .cta-sub {
    color: var(--muted);
    max-width: 480px;
    margin: 0 auto 36px;
    line-height: 1.75;
    font-size: 0.96rem;
  }
  .cta-btns { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }
  .cta-btn {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    background: var(--brand);
    color: #fff;
    font-weight: 700;
    border-radius: 10px;
    padding: 14px 30px;
    text-decoration: none;
    transition: all 0.3s;
  }
  .cta-btn:hover {
    box-shadow: 0 0 30px rgba(189, 56, 8, 0.4);
    transform: translateY(-2px);
    color: #fff;
  }
  .cta-btn-outline {
    display: inline-flex;
    align-items: center;
    gap: 10px;
    border: 1.5px solid rgba(255,255,255,0.2);
    color: var(--text);
    font-weight: 600;
    border-radius: 10px;
    padding: 14px 30px;
    text-decoration: none;
    transition: all 0.3s;
  }
  .cta-btn-outline:hover { border-color: var(--brand); color: var(--brand); }

  @media (max-width: 768px) {
    .cta { padding: 0 24px 60px; }
    .cta-inner { padding: 50px 28px; }
  }
</style>
